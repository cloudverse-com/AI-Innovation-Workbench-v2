# AI Innovation Workbench — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0              # Modern async web framework
uvicorn[standard]>=0.32.0    # ASGI server (use --reload for development)
python-multipart>=0.0.12     # Required for FastAPI file upload support
aiofiles>=24.1.0             # Async file reading for static file serving

# ── Microsoft Agent Framework ──────────────────────────────────────────────
agent-framework              # Agent + FoundryChatClient — core agent SDK
azure-identity>=1.19.0       # Credential types (AzureKeyCredential, AzureCliCredential)

# ── Semantic Kernel (Demo 11) — installed SEPARATELY, NOT in this file ──────
# semantic-kernel pins azure-ai-projects<2.0, but agent-framework-foundry needs >=2.0.
# They cannot be resolved together by a single `pip install -r` (ResolutionImpossible).
# Install in ordered steps AFTER this file, then force azure-ai-projects back to 2.x:
#   pip install -r requirements.txt
#   pip install "semantic-kernel>=1.42.0"
#   pip install "azure-ai-projects>=2.0.0,<3.0"
# The Dockerfile already runs these in order.

# ── Configuration ──────────────────────────────────────────────────────────
python-dotenv>=1.0.1         # Loads .env file into environment variables

# ── Data Validation ────────────────────────────────────────────────────────
pydantic>=2.0.0              # Request/response validation, JSON schema generation

# ── Document Processing ────────────────────────────────────────────────────
Pillow>=11.0.0               # Image processing for vision mode
pymupdf>=1.24.0              # Fast PDF text extraction for Demo 08 (no ML models)
liteparse>=2.0.0             # Rust-powered local spatial PDF parser for Demo 10

# ── Azure Content Understanding (Demo 06) ──────────────────────────────────
azure-ai-contentunderstanding>=1.1.0  # Medical report analysis via Content Understanding

# ── HTTP Client ────────────────────────────────────────────────────────────
httpx>=0.28.0                # Async HTTP client (used internally by Azure SDK)

# ── Azure Blob Storage (Demo 07 — Contract Comparison) ─────────────────────
azure-storage-blob>=12.0.0   # Upload contract PDFs and generate SAS URLs
